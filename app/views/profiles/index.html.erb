<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <script src="events.js" type="javascript.js"></script>
    <title>Twitter Fetcher</title>
  </head>
  <body>
    <h1>Twitter Fetcher</h1>
    <h3>Crawling in my rails, these tweets they will not heal!</h3>

    <h4>Search: </h4>
    <%= form_tag(profiles_path, method: :get) do %>
      <%= text_field_tag(:search, params[:search]) %>
      <%= submit_tag ("Search") %>
    <% end %>

    <!-- <form action="/search" method="POST">
      <label for="input">Search profile: <input type="text" name="search" placeholder="gameideamachine"/></label>
      <button type="submit" class="fetcher">Search profile</button>
    </form> -->
    <br>
    <%= link_to "Add new profile", "/profiles/new" %>
    <hr color="#D0D0D0" size='2px'>
    <div class="container">
      <ul id="profiles">
        <% @profiles.each do |profile| %>
          <li><a href="profiles/<%=profile.id%>"><%= profile.username %></a>
          <a href=<%=profile.url%>><%=profile.url%></a>
          <%= link_to "edit", edit_profile_path(profile[:id]) %>
          <%= link_to "delete", profile, method: :delete, data: { confirm: "You sure?" }%></li>
        <% end %>
      </ul>
    </div>




  </body>
</html>
